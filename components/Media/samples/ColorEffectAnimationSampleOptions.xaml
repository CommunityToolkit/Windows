<Page
	x:Class="MediaExperiment.Samples.ColorEffectAnimationSampleOptions"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<StackPanel x:Name="RootOptionsPanel" Spacing="8">
		<Button Click="OnStartAnimationButton_Clicked" Content="Start animation" />

		<Button Click="OnStopAnimationButton_Clicked" Content="Stop animation" />

		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup x:Name="OrientationStates">
				<VisualState x:Name="DefaultLayout">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint1008Plus}" />
					</VisualState.StateTriggers>
				</VisualState>

				<VisualState x:Name="VerticalLayout">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint641Plus}" />
						<AdaptiveTrigger MinWindowWidth="0" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="RootOptionsPanel.Orientation" Value="Horizontal" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
	</StackPanel>
</Page>
